version: "3.9"

services:
  api:
    build: .
    container_name: smartcoach-api
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - WATCHFILES_FORCE_POLLING=true
    command: uvicorn api:app --host 0.0.0.0 --port 8080 --reload